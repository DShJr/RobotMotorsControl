# RobotMotorsControl
Home work 4.5

## Общее описание проекта
Цель проекта — создать базовый скрипт для управления двигателями робота.

## Описание работы с Git (Отчёт по практике 4.5)
1. **Репозиторий 'RobotMotorsControl'** был успешно `создан` на GitHub и клонирован локально.
2. **Последовательность сохранения изменений:** `git add` -> `git commit` (для сохранения локально) -> `git push` (для отправки на сервер github).
3. **Взаимодействие с ветками:**
    * Создана ветка `feature/smooth-speed` для разработки новой функции.
    * Разработанная функция была зафиксирована в этой ветке.
    * После завершения работы ветка была успешно объединена (`git merge`) с веткой `main` и после удалена.

### Пояснение к пункту 3
**Слияние изменений** было выполнено напрямую командой `git merge` локально, а не через интерфейс GitHub: `Pull Request`.

## Основные изменения в коде
* **motors_control.py:** Добавлена функция `set_smooth_speed(speed)` для плавного изменения скорости моторов, что улучшает управляемость роботом.

## Подтверждение хода выполнения работы
Изображения выполнения работы в терминале, прилагаю.
